import { DisplayLeftPointsCaracteristicsPipe } from './display-left-points-caracteristics.pipe';

describe('DisplayLeftPointsCaracteristicsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayLeftPointsCaracteristicsPipe();
    expect(pipe).toBeTruthy();
  });
});
