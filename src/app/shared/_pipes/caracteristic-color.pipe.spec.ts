import { CaracteristicColorPipe } from './caracteristic-color.pipe';

describe('CaracteristicColorPipe', () => {
  it('create an instance', () => {
    const pipe = new CaracteristicColorPipe();
    expect(pipe).toBeTruthy();
  });
});
